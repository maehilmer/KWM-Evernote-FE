import { LabelFormErrorMessages } from './label-form-error-messages';

describe('LabelFormErrorMessages', () => {
  it('should create an instance', () => {
    expect(new LabelFormErrorMessages()).toBeTruthy();
  });
});
